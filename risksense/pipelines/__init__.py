"""PySpark ETL pipelines.

Run locally with ``local[*]`` (default) or on Databricks Community Edition —
the same code path works in both because everything goes through
:func:`risksense.pipelines.spark_session.get_spark`.
"""
